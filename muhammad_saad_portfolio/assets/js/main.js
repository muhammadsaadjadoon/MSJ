const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('.nav-links');toggle.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const lines=['I build intelligent AI systems 🤖','I engineer Machine Learning solutions 📊','I develop Generative AI applications ✨','I connect AI models with full-stack products 💻'];const t=document.getElementById('typed');let li=0,ci=0,del=false;function loop(){if(!t)return;let s=lines[li];t.textContent=s.slice(0,ci);if(!del&&ci<s.length){ci++;setTimeout(loop,45)}else if(!del){del=true;setTimeout(loop,1400)}else if(ci>0){ci--;setTimeout(loop,24)}else{del=false;li=(li+1)%lines.length;setTimeout(loop,240)}}if(t)setTimeout(loop,350);
const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');ro.unobserve(e.target)}}),{threshold:.07});document.querySelectorAll('.reveal').forEach(e=>ro.observe(e));
const secs=[...document.querySelectorAll('main section[id]')],links=[...document.querySelectorAll('.nav-links a')];const so=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-35% 0px -55%',threshold:.01});secs.forEach(s=>so.observe(s));
const topBtn=document.querySelector('.to-top');addEventListener('scroll',()=>topBtn.classList.toggle('show',scrollY>500));